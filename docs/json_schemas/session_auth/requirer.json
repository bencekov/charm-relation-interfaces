{
  "title": "RequirerSchema",
  "description": "Requirer schema for kratos_endpoints.",
  "type": "object",
  "properties": {
    "unit": {
      "$ref": "#/definitions/BaseModel"
    },
    "app": {
      "$ref": "#/definitions/SessionAuthRequirer"
    }
  },
  "required": [
    "app"
  ],
  "definitions": {
    "BaseModel": {
      "title": "BaseModel",
      "type": "object",
      "properties": {}
    },
    "SessionAuthRequirer": {
      "title": "SessionAuthRequirer",
      "type": "object",
      "properties": {
        "allowed_return_urls": {
          "title": "Allowed Return Urls",
          "description": "List of allowed return urls for used by Oathkeeper clients",
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      },
      "required": [
        "allowed_return_urls"
      ]
    }
  }
}